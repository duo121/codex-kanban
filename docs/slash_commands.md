# Slash commands

For an overview of Codex CLI slash commands, see [this documentation](https://developers.openai.com/codex/cli/slash-commands).

This fork adds one local command:

- `/kb`: open the kanban board picker for the current window. It is separate from `/resume`, which keeps the upstream global-session semantics.
